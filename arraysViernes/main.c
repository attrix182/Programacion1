#include <stdio.h>
#include <stdlib.h>
#include "biblio.h"
#define TAM 10

int const X = 10;

int main()
{

    int miArray[TAM] = {};
    int indice;

    mostrarArray(miArray, TAM);
    cargarArray(miArray, TAM)
    indice = buscarValor(miArray, )


    return 0;
}


